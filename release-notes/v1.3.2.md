## Fixes

- `Setup.exe` now has an actual icon instead of a blank/generic one -- no more looking like a
  suspicious unbranded executable.
- The Releases page now also includes a version-named copy of the installer (e.g.
  `PublishTool.Gui-Setup-v1.3.2.exe`), so it's clear which version you're downloading when
  grabbing it by hand.

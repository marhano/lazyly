## Fixes

- The update check can now use an authenticated GitHub token when one's configured, raising its
  rate limit from 60/hour to 5000/hour -- the real fix for "Update check failed: ... 403 (rate
  limit exceeded)" beyond just checking less often. No visible change if a token isn't configured
  yet; it falls back to unauthenticated checks exactly as before.

<ui:FluentWindow x:Class="PublishTool.Gui.CredentialPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Credentials Required" Height="280" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="MessageTextBlock" TextWrapping="Wrap" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Text="Password" Margin="0,0,0,6"/>
        <ui:PasswordBox Grid.Row="2" x:Name="PasswordBox" Margin="0,0,0,12"/>

        <CheckBox Grid.Row="3" x:Name="RememberCheckBox" Content="Save this password (encrypted, this Windows user only)"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <ui:Button Content="Cancel" Click="CancelButton_Click" Margin="0,0,8,0"/>
            <ui:Button Content="Connect" Appearance="Primary" Click="ConnectButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>

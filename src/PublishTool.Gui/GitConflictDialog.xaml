<ui:FluentWindow x:Class="PublishTool.Gui.GitConflictDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Height="480" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="MessageTextBlock" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,14"/>

        <TextBlock Grid.Row="1" x:Name="FilesHeaderTextBlock" FontWeight="SemiBold" Margin="0,0,0,6"/>
        <ListBox Grid.Row="2" x:Name="FilesListBox" Margin="0,0,0,14"/>

        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="Commit message (only used if you choose Commit)" Margin="0,0,0,6"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            <ui:TextBox x:Name="CommitMessageTextBox"/>
        </StackPanel>

        <WrapPanel Grid.Row="4" HorizontalAlignment="Right">
            <ui:Button x:Name="CheckoutAnywayButton" Content="Checkout anyway" Margin="0,0,8,8"
                       Click="CheckoutAnywayButton_Click" Visibility="Collapsed"/>
            <ui:Button Content="Discard changes" Click="DiscardButton_Click" Margin="0,0,8,8"/>
            <ui:Button Content="Stash changes" Click="StashButton_Click" Margin="0,0,8,8"/>
            <ui:Button Content="Commit changes" Appearance="Primary" Click="CommitButton_Click" Margin="0,0,8,8"/>
            <ui:Button Content="Cancel" Click="CancelButton_Click" Margin="0,0,0,8"/>
        </WrapPanel>
    </Grid>
</ui:FluentWindow>

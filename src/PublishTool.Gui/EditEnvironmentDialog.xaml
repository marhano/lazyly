<ui:FluentWindow x:Class="PublishTool.Gui.EditEnvironmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Environment" Height="460" Width="420"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Environment" Margin="0,0,0,6"/>
        <ComboBox Grid.Row="1" x:Name="EnvironmentNameComboBox" IsEditable="True" Margin="0,0,0,14"/>

        <ui:ToggleSwitch Grid.Row="2" x:Name="AutoCreateSiteToggle" Content="Auto-create IIS site" Margin="0,0,0,14"
                         ToolTip="Automatically creates the IIS site (and its own app pool) the first time this environment is deployed to, if it doesn't already exist. Requires at least one binding below."/>

        <TextBlock Grid.Row="3" Text="Site bindings" Margin="0,0,0,6"/>
        <DataGrid Grid.Row="4" x:Name="BindingsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Protocol}" Width="80"/>
                <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="110"/>
                <DataGridTextColumn Header="Port" Binding="{Binding Port}" Width="70"/>
                <DataGridTextColumn Header="Host name" Binding="{Binding HostName}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,14">
            <ui:Button Content="Add binding" Icon="{ui:SymbolIcon Add24}" Width="130" Click="AddBindingButton_Click"/>
            <ui:Button Content="Remove binding" Icon="{ui:SymbolIcon Delete24}" Width="140" Margin="10,0,0,0" Click="RemoveBindingButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button Content="Cancel" Click="CancelButton_Click" Margin="0,0,8,0"/>
            <ui:Button Content="Save" Appearance="Primary" Icon="{ui:SymbolIcon Save24}" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>

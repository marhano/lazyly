<ui:FluentWindow x:Class="PublishTool.Gui.DeploymentHistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Deployment History" Height="480" Width="640"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleTextBlock" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <ui:TextBox Grid.Row="1" x:Name="SearchTextBox" Margin="0,0,0,10"
                    PlaceholderText="Search version, environment, deployed by..."
                    TextChanged="SearchTextBox_TextChanged"/>

        <DataGrid Grid.Row="2" x:Name="HistoryDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="120"/>
                <DataGridTextColumn Header="Environment" Binding="{Binding EnvironmentName}" Width="140"/>
                <DataGridTextColumn Header="Deployed At" Binding="{Binding DeployedAtUtc}" Width="180"/>
                <DataGridTextColumn Header="Deployed By" Binding="{Binding DeployedBy}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <ui:Button Content="Close" Appearance="Primary" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>

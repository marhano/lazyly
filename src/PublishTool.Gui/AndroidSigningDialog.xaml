<ui:FluentWindow x:Class="PublishTool.Gui.AndroidSigningDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Android Signing" Height="420" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,16"
                   Text="Same fields as Android Studio's &quot;Generate Signed Bundle / APK&quot; dialog. Used only for release builds -- stored locally on this machine, passwords encrypted, never shared with your team."/>

        <TextBlock Grid.Row="1" Text="Key store path" Margin="0,0,0,6"/>
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ui:TextBox Grid.Column="0" x:Name="KeystorePathTextBox" Margin="0,0,6,0" PlaceholderText="C:\keys\release.jks"/>
            <ui:Button Grid.Column="1" Content="Browse..." Icon="{ui:SymbolIcon FolderOpen24}" Width="100" Click="BrowseKeystorePath_Click"/>
        </Grid>

        <TextBlock Grid.Row="3" Text="Key store password" Margin="0,0,0,6"/>
        <ui:PasswordBox Grid.Row="4" x:Name="KeystorePasswordBox" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="5" Text="Key alias" Margin="0,0,0,6"/>
        <ui:TextBox Grid.Row="6" x:Name="KeyAliasTextBox" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="7" Text="Key password" Margin="0,0,0,6"/>
        <ui:PasswordBox Grid.Row="8" x:Name="KeyPasswordBox" Margin="0,0,0,0" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <ui:Button Content="Clear" Click="ClearButton_Click" Margin="0,0,8,0"
                       ToolTip="Removes any saved signing details for this project -- release builds fall back to the native project's own signingConfig, if any."/>
            <ui:Button Content="Cancel" Click="CancelButton_Click" Margin="0,0,8,0"/>
            <ui:Button Content="Save" Appearance="Primary" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>

<UserControl x:Class="PublishTool.Gui.ReleaseNoteItemsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" d:DesignWidth="320">
    <Border Padding="12" CornerRadius="6" Background="{DynamicResource ControlFillColorDefaultBrush}">
        <StackPanel>
            <TextBlock x:Name="HeaderTextBlock" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal">
                <ui:TextBox x:Name="InputTextBox" Width="200" PlaceholderText="Add an item..."
                            KeyDown="InputTextBox_KeyDown"/>
                <ui:Button Content="Add" Icon="{ui:SymbolIcon Add24}" Width="80" Margin="8,0,0,0"
                           Click="AddButton_Click"/>
            </StackPanel>
            <ListBox x:Name="ItemsListBox" Height="80" Margin="0,8,0,0"/>
            <ui:Button Content="Remove selected" Icon="{ui:SymbolIcon Delete24}" HorizontalAlignment="Left"
                       Margin="0,8,0,0" Click="RemoveButton_Click"/>
        </StackPanel>
    </Border>
</UserControl>

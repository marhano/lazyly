<ui:FluentWindow x:Class="PublishTool.Gui.FirewallAuditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Firewall Audit Trail" Height="480" Width="720"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Every rule added, edited, or removed here -- who did it and when" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <ui:TextBox Grid.Row="1" x:Name="SearchTextBox" Margin="0,0,0,10"
                    PlaceholderText="Search rule name, action, by..."
                    TextChanged="SearchTextBox_TextChanged"/>

        <DataGrid Grid.Row="2" x:Name="AuditDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding PerformedAtUtc, Converter={StaticResource UtcToPhTimeConverter}}" Width="160"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="80"/>
                <DataGridTextColumn Header="Rule Name" Binding="{Binding RuleName}" Width="*"/>
                <DataGridTextColumn Header="Protocol" Binding="{Binding Protocol}" Width="80"/>
                <DataGridTextColumn Header="Port" Binding="{Binding Ports}" Width="120"/>
                <DataGridTextColumn Header="By" Binding="{Binding PerformedBy}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <ui:Button Content="Close" Appearance="Primary" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
